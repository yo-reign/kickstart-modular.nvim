-- Currently disabled because it doesn't play nice with cmake-tools.lua
-- Too use together, you need to nvim directly into CMakeLists.txt or else it breaks: 'nvim CMakeLists.txt'

-- return {
--   'stevearc/oil.nvim',
--   opts = {},
--   -- Optional dependencies
--   dependencies = { 'nvim-tree/nvim-web-devicons' },
-- }

return {}
